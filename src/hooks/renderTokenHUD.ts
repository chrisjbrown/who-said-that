export default function createToken(token, data) {
   console.log(token);
   // html[0].insertAdjacentHTML(
   //    "afterend",
   //    `
   //      <div id="WSTChatBubble"><i class="fa-solid fa-heart fa-beat"></i></div>
   //    `
   // );
}
